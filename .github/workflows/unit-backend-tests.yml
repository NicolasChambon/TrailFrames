name: Backend Unit Tests

on:
  pull_request:
    paths:
      - "backend/**"
      - ".github/workflows/unit-backend-tests.yml"
  push:
    branches:
      - dev
      - staging
      - main
    paths:
      - "backend/**"

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Show npm debug log if failure
        if: failure()
        run: |
          ls -l /home/runner/.npm/_logs
          cat /home/runner/.npm/_logs/*debug-0.log

      - name: Run unit tests
        run: npm run test:unit
